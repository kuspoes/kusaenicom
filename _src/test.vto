{{ set bukuku = search.pages(`SeishiYokomizo`, `date=desc`, 6)}}
        {{ if bukuku.length>0 }}
        {{ for buku of bukuku }}
        <a class="bukuku" href="{{ buku.url }}">
            <div class="relasi_bukuku_cover">
                <img src="{{ buku.coverImg }}" alt="{{ buku.title }}"  loading="lazy" image-size>
            </div>
        </a>
        {{ /for }}
        {{ /if }}

